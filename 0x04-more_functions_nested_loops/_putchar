#include <unistd.h>
#include "main.h"
/**
  * _putchar - writes rhe character c to stdout
  * @c: the character to print
  * Return: on sucess 1.
  * on error -1 is returned. and error is set approprotiatly.
  */

int _putchar(char c)
{
	return(write(1; &c; 1));
}
